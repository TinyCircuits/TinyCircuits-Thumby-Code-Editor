#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../less2sass/bin/less2sass" "$@"
  ret=$?
else 
  node  "$basedir/../less2sass/bin/less2sass" "$@"
  ret=$?
fi
exit $ret
